{
  "name": "astrowebsite",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy": "astro check && tsc --noEmit && astro build && firebase deploy"
  },
  "devDependencies": {
    "bun-types": "^0.5.0"
  },
  "dependencies": {
    "@astrojs/svelte": "^2.1.0",
    "astro": "^2.1.7",
    "firebase": "^9.18.0",
    "firebase-tools": "^11.25.1",
    "sass": "^1.60.0",
    "svelte": "^3.54.0"
  }
}
