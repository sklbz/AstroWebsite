{
  "name": "astrowebsite",
  "module": "index.ts",
  "type": "module",
	"scripts": {
		"build": "astro check && tsc --noEmit && astro build"
	},
  "devDependencies": {
    "bun-types": "^0.5.0"
  },
  "dependencies": {
    "astro": "^2.1.7"
  }
}